# Step 1: Check the standard Unix password
auth    required    pam_unix.so nullok

# Step 2: Check the Google Authenticator code
auth    required    pam_google_authenticator.so secret=/home/${USER}/.google_authenticator authtok_prompt=pin
